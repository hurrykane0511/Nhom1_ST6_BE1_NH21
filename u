// if(kw){
        //     query += '&keyword=' + kw.textContent + '';
        // }


        if (isset($_GET['keyword'])) {
        if ($where != '') {
            $where .= 'AND (' . ' pf_name like ' . 'concat("%", ? , "%")' . ') ';
        } else {
            $where .= '(' . ' pf_name like ' . 'concat("%", ? , "%")' . ') ';
        }
        $bind .= 's';
        $finalArr[] =  $_GET['keyword'];
    }
